# Changelog

All notable changes to this project will be documented in this file.

The format is based on Keep a Changelog, and this project follows Semantic Versioning.

## [Unreleased]

### Added

- First implementation of file-backed profile storage.
- CLI commands for capture, sync, list, current, use, inspect, remove, and default SDK selection.
- Python `ProfileManager` and `build_sdk_env()` helpers.
- OAuth refresh handling and `reauth_required` state tracking.
- Unit tests, linting, typing, and CI configuration.

